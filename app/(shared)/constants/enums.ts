export enum Urls {
  HOME = '/',
  HOMEWARE = '/homeware/products',
  LOGIN = '/login',
}

export enum Path {
  PRODUCTS = 'products',
  SIGN_UP = 'signup',
}

export enum Method {
  GET = 'GET',
  POST = 'POST',
  PUT = 'PUT',
  DELETE = 'DELETE',
}

export enum ProductMode {
  NEW = 'new',
  SALES = 'sales',
}
